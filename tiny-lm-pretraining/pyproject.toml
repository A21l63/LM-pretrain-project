[project]
name = "tiny-lm-pretraining"
version = "0.1.0"
description = "Educational skeleton for pretraining tiny language models from scratch"
requires-python = ">=3.10"
dependencies = [
    "datasets>=4.8.5",
    "pytest>=9.0.3",
    "torch>=2.11.0",
    "tqdm>=4.67.3",
    "transformers>=5.9.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
